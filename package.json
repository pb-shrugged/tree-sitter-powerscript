{
  "name": "@pb-shrugged/tree-sitter-powerscript",
  "version": "0.0.2",
  "description": "Powerscript grammar for tree-sitter",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pb-shrugged/tree-sitter-powerscript.git"
  },
  "license": "MIT",
  "author": {
    "name": "Jose Cagnini"
  },
  "main": "bindings/node",
  "types": "bindings/node",
  "keywords": [
    "incremental",
    "parsing",
    "tree-sitter",
    "powerbuilder",
    "powerscript"
  ],
  "files": [
    "grammar.js",
    "tree-sitter.json",
    "binding.gyp",
    "prebuilds/**",
    "bindings/node/*",
    "queries/*",
    "src/**",
    "*.wasm"
  ],
  "dependencies": {
    "node-addon-api": "^8.5.0",
    "node-gyp-build": "^4.8.4"
  },
  "devDependencies": {
    "prebuildify": "^6.0.1",
    "tree-sitter": "^0.25.0",
    "tree-sitter-cli": "^0.26.7",
    "eslint": "^9.35.0",
    "eslint-config-treesitter": "^1.0.2"
  },
  "peerDependencies": {
    "tree-sitter": "^0.25.0"
  },
  "peerDependenciesMeta": {
    "tree-sitter": {
      "optional": true
    }
  },
  "scripts": {
    "install": "node-gyp-build",
    "prestart": "tree-sitter build --wasm",
    "start": "tree-sitter playground",
    "test": "node --test bindings/node/*_test.js",
    "ts:start": "npm run ts:generate && npm run ts:run",
    "ts:generate": "tree-sitter generate",
    "ts:run": "tree-sitter build --wasm && tree-sitter playground -q",
    "ts:test": "tree-sitter test",
    "ts:parse:playground": "npm run ts:generate && tree-sitter parse ./examples/playground.srf",
    "lint": "eslint grammar.js",
    "lint:fix": "eslint grammar.js --fix"
  }
}
